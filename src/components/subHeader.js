function SubTitle(props){
    return(
      <h2 style= {SubHeaderStyle}>{props.title}</h2>
    );
  }

  export default SubTitle

    // Styling
let SubHeaderStyle = {
    margin: "1em 0 0 0"
  };
    
  